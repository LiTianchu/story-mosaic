# Server env vars
# Copy to server/.env and fill in values.

PORT=5000
MONGO_URI=mongodb://localhost:27017/story-mosaic

# Firebase Admin credentials (choose ONE approach)
# 1) Point to a local JSON file (recommended)
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json
# (or) GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# 2) Inline JSON (useful for CI/CD secret managers)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Firebase Storage bucket (optional; inferred from service account if omitted)
FIREBASE_STORAGE_BUCKET=
